// examples/demo.ltl — runnable demo for ltl_totp_cli

import std::io
use ltl_totp_cli as lib

fn main() {
    io::println("ltl_totp_cli demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}